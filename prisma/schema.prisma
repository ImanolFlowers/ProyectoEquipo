generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}


enum user_role {
  ARBITRO
  ENTRENADOR
  VISITANTE
}


model user {
  id       String    @id
  name     String
  username String    @unique(map: "User_username_key")
  email    String    @unique(map: "User_email_key")
  image    String?
  password String
  active   Boolean   @default(true)
  createAt DateTime  @default(now())
  updateAt DateTime?
  telefono String?
  apellido String?
  role     user_role @default(VISITANTE)

    // equipos  Equipo[]  @relation("EntrenadorEquipos")
}

model temporada {
  id        String   @id
  nombre    String
  createdAt DateTime @default(now())
  updatedAt DateTime
  activa    Boolean  @default(true)
}


// model Equipo {
//   id           String   @id @default(cuid())
//   nombre       String
//   escudo       String?
//   localidad    String?
//   entrenadorId String
//   entrenador   user     @relation("EntrenadorEquipos", fields: [entrenadorId], references: [id])

//   active       Boolean  @default(true)
//   createdAt    DateTime @default(now())
//   updatedAt    DateTime?
// }